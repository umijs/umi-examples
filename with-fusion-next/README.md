
# with-fusion-next

这是 umi + [fusion next](https://github.com/alibaba-fusion/next) 的例子。

## 使用步骤

> `@alife/next` 同理。

1. 安装 babel-plugin-import 和 @alifd/next 依赖
2. 在 `.umirc.js` 里通过 `extraBabelPlugins` 配置 @alifd/next 的按需编译
3. 在代码里 import @alifd/next 的组件使用

